#include <stdio.h>
int main()
{
    int i, t, a, b, c, temp;
    scanf("%d", &t);
    for(i=1; i<=t; i++)
    {
        scanf("%d%d%d", &a, &b, &c);
        if(a<b)
        {
            if(b>c){

            }
        }
    }
}
